#include <stdio.h>
//Aula de Ciclo for | Laço for | Repetição for

int main (void) { // int main vazio: () ou void: (void) é a mesma coisa

    int contador;
    int numero = 1;

    for (contador = 1; contador <= 10; contador = contador + 1) {
        
        printf ("teste\n");
    }

// no "for", a primeira variável representa o inicio do ciclo
// a do meio representa o limite 
// e a terceira representa que enquanto a variável não atingir o limite, o que está escrito ali ira acontecer


return 0;
}